package flex.admin.suite.tests.theory.object {
    public class MonthLength {
        public var currentDate:Date;
        public var days:int;

        public function MonthLength(dateOne:Date, between:Number) {
            currentDate = dateOne;
            days = between;
        }
    }
}